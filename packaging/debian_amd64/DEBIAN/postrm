#!/bin/sh

set -e

delete() {
  if [ -f "${1}" ] ; then
    rm "${1}"
  fi
}

delete_service_files() {
  delete /etc/systemd/fio-bco.service.wants/fio-bco-rest.service
  delete /lib/systemd/system/fio-bco-rest.service

  delete /etc/systemd/fio-bco.service.wants/fio-bco-import@.service
  delete /lib/systemd/system/fio-bco-import@.service

  delete /etc/systemd/fio-bco.service.wants/fio-bco.path
  delete /lib/systemd/system/fio-bco.path

  delete /etc/systemd/multi-user.target.wants/fio-bco.service
  delete /lib/systemd/system/fio-bco.service
}

delete_displace_files() {
  delete /etc/init/fio-bco.conf
}

case "$1" in

  purge)
    delete_service_files
    delete_displace_files
    (systemctl daemon-reload >/dev/null 2>&1 || :)
    (systemctl reset-failed >/dev/null 2>&1 || :)
  ;;

  remove)
    (systemctl -a -t service --no-legend  | awk '$1 ~ /fio-bco/ { print $1 }' | xargs -I {} systemctl stop {} 2> /dev/null || :)
    delete_displace_files
    (systemctl daemon-reload >/dev/null 2>&1 || :)
  ;;

  upgrade|deconfigure)
    (systemctl stop fio-bco >/dev/null 2>&1 || :)
  ;;

  remove|failed-upgrade|abort-install|abort-upgrade|disappear)
  ;;

  *)
    (>&2 echo "postrm called with unknown argument \`$1'")
    exit 1
  ;;

esac

exit 0
